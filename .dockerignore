# Docker 构建忽略清单 —— examples/ 下的示例 Dockerfile 都以**仓库根**为构建上下文
# （框架当前版本未发布到 npm，镜像里先从源码构建框架，见 examples/complete/Dockerfile）。
# 注意：Docker 只认构建上下文根上的 .dockerignore，放在示例目录里不会生效。
.git
.gitignore
.github

# 依赖与产物：镜像里自己装 / 自己构建
node_modules
**/node_modules
dist
**/dist
.astro
**/.astro
.wrangler
**/.wrangler

# 与构建无关的重物
packages/website
*.zip

# 本地状态与密钥
*.log
.DS_Store
.env
.env.local
**/.env
**/.env.local
**/*.db
**/*.db-wal
**/*.db-shm
*.tgz
