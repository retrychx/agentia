# 复制成 .env 再填值（.env 已 gitignore）。只影响 npm start（真模型）；npm run demo 零 key 可跑。

# Anthropic 协议端点（框架默认 client 读这两个）
ANTHROPIC_API_KEY=sk-ant-...
# ANTHROPIC_BASE_URL=https://api.anthropic.com   # 任何 Anthropic 兼容端点都行（如 DeepSeek 的 /anthropic）

# 缺省模型：不设则用示例自带的 deepseek-v4-flash（已配 priceOverrides，成本可算）
# AGENTIA_MODEL=claude-sonnet-5
