/**
 * dev 环的**数据**声明（只有数据，没有逻辑）—— `agentia dev` 存在才读，缺省零样板。
 *
 * 为什么是「数据文件」而不是「逻辑文件」：逻辑副本会**漂移**（CLI 修了 bug、加了能力，
 * 工程里那份副本一个都不会变，而且没人会主动维护它），数据声明不会。
 * ⇒ 规则：`dev.ts`（逻辑）不要；`dev.config.ts`（数据）可以。
 *
 * ⚠️ 这个文件**只影响开发期的调试环**，生产路径（`npm start` → dist/main.js）完全不读它。
 * 所以「每个能力各自要不要多轮」这种只在调试时成立的知识，放这里最合适
 * （生产里没有能力选择器：一次对话就是一个 session，模型在对话内自己路由）。
 */
export default {
  /**
   * 这些能力按**多轮**调试（累积 session，模型看得见前面的对话）；未列出的按单轮。
   *
   * 为什么要按能力声明而不是一个全局开关：同一个工程里两种形态都要 ——
   * 「行程规划」要（「改成 3 天」「住宿换民宿」是常态），
   * 「code-review」「查天气」不要（每次独立，把上一个仓库的评审串进来会误导）。
   *
   * 面板上那个多轮开关的**初始值** = 所选能力声明的 **OR**（只要有一个声明多轮就默认多轮），
   * 且开关上会标出来源（如「多轮·trip-planner」）—— 混选时默认值有歧义，
   * 标注来源是为了不让你面对一个「为什么这次带了上下文」的谜。开关可以随时覆盖。
   *
   * 例：multiTurn: ['trip-planner'],
   */
  multiTurn: [],

  /**
   * 面板上「工作目录」的初始值。缺省 = 项目根。
   * 它只是**初始值** —— 面板随时能改，改的是这次 run 让工具去看哪棵树。
   * 例：workdir: '/Users/me/work/another-repo',
   */

  /**
   * 单次 run 的预算护栏（缺省 $1 / 200000 token）。
   * 面板上「点一下就跑一次真 agent」，而真 agent 真花钱 —— 一个跑偏的循环就是一张账单。
   * 这里可以按工程调；调小更安全，调大更放得开。
   * 例：budget: { maxCostUsd: 5, maxTotalTokens: 1_000_000 },
   */
};
